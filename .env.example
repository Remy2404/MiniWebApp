# Environment Template for Telegram Mini Web App

# Backend API URL (REQUIRED for production)
# Set this to your actual backend server URL when deploying
# Examples:
# VITE_BACKEND_URL=https://your-backend-domain.com
# VITE_BACKEND_URL=https://your-app.herokuapp.com
# VITE_BACKEND_URL=https://your-app.railway.app

VITE_BACKEND_URL=

# Development fallback (optional)
# VITE_BACKEND_URL_DEV=http://localhost:8000

# Security Notes:
# 1. Never commit actual production URLs to version control
# 2. Set VITE_BACKEND_URL as an environment variable in your deployment platform
# 3. Use different values for staging and production environments
# 4. Regularly rotate any tokens or keys in the backend URL
